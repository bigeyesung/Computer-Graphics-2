For this project, you will be implementing a simple raytracer that can handle shadows and reflections in a basic scene containing primitive shapes. All rendering can be done with software, only using OpenGL to display the final image to the screen.
creating a ray tracer capable of rendering a set of primitive shapes.
